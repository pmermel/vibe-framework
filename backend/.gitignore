# Generated by predev — copy of infrastructure/container-apps-env.json for local dev server.
# The canonical source is /infrastructure/container-apps-env.json at the repo root.
# Production: dist/infrastructure/ is populated by postbuild and included in the Docker image.
src/infrastructure/
